# Optimized Lambda requirements - only essential packages
boto3>=1.26.0
botocore>=1.29.0

# Document processing (Lambda compatible)
PyPDF2>=3.0.0  # Lightweight PDF processing for Lambda
sentence-transformers>=2.2.0  # For embeddings

# Vector search (lightweight alternatives)
faiss-cpu>=1.7.4  # CPU-only FAISS for Lambda
scikit-learn>=1.3.0  # For BM25 and preprocessing

# Utilities
numpy>=1.24.0
psutil>=5.9.0  # For memory monitoring