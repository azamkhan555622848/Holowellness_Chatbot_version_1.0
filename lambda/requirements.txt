# Lambda requirements - hybrid BM25 + TF-IDF semantic search
boto3>=1.26.0
botocore>=1.29.0

# Document processing (Lambda compatible)
PyPDF2>=3.0.0

# Search and preprocessing (Lambda compatible - no PyTorch needed)
scikit-learn>=1.3.0  # For TF-IDF, cosine similarity, and BM25
numpy>=1.24.0  # Core numerical computing
scipy>=1.10.0  # Additional scientific computing

# Utilities
psutil>=5.9.0  # For memory monitoring